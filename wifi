#!/usr/bin/env bash
#------------------------------------------------------------------------

INTERFACE="${BLOCK_INSTANCE:-wlp3s0}"

#------------------------------------------------------------------------

NAME=$(iw dev $INTERFACE info | grep ssid | cut -d ' ' -f 2)

#------------------------------------------------------------------------

echo "$NAME"
echo "$NAME"
